export enum UnauthorizedStackName {
  PROFILE =  'Profile',
  LANDING = 'Landing',
  SCANNER = 'Scanner',
}

export enum UnAuthProfileStackName {
  MAIN = "Main",
  USER = "Login",
  ORDER_HISTORY = "OrderHistory",
  FEEDBACK = "Feedback",
}

export enum UnAuthScannerStackName {
  CAMERA = "Camera",
  SCAN_RESULT = "ScanResult",
}

export enum AuthorizedStackName {
  MENU = 'Menu',
  MANAGE_PRODUCT = 'ManageProducts',
  MANAGE_EMPLOYEE = 'ManageEmployees',
  MANAGE_BILL = 'ManageBills',
  SALE_STATISTIC = 'SaleStatistic',
  CREATE_BILL = 'CreateBill'
}
